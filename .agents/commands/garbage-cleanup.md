Run a cleanup scan using `.agents/skills/garbage-cleanup/SKILL.md`.

Prioritize dead code, stale scaffolding, duplicate helpers, layer drift, hardcoded workflow decisions, unused dependencies, and unsafe logging. Report safe removals separately from changes that require a plan.

