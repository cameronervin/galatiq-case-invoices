Debug a bug using `.agents/skills/bug-squasher/SKILL.md`.

Defaults:
- Read `README.md`, `backstage/architecture/overview.md`, and relevant code first.
- Rank hypotheses, verify them with targeted evidence, and patch the smallest safe surface.
- Preserve deterministic fixtures, structured errors, approval safeguards, and JSON-safe worker contracts.
- Never log invoice contents, vendor details, prompts, model payloads, tokens, or API keys.

Default prompt: Debug the reported issue with hypothesis-driven analysis, prove the root cause, apply a minimal fix, and run the relevant backend or frontend checks.

