Implement a feature using `.agents/skills/implement-feature/SKILL.md`.

Read `README.md`, `backstage/architecture/overview.md`, and relevant code before editing. Use TDD for non-trivial workflow, agent, API, worker, persistence, and frontend behavior. Keep changes typed and update docs when behavior changes.

Default prompt: Implement the requested invoice-processing feature with focused tests, preserve local deterministic behavior and approval safeguards, update docs, and run the relevant checks.

